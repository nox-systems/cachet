// The lane's stand-in for a built console bundle. Its only job is to be a
// file with a hashed name, so the lane can prove a file is served by name
// where a route is served the shell.
export const lane = "console";
